install tweepy cmd = pip install tweepy
install textblob cmd = pip install textblob
goto https://developer.twitter.com/ and create an account. you will get consumer_key,
consumer_secret, access_token, access_token_secret from there.
put that data in auth_data.py files. Now you can run these jypyter notebook cells